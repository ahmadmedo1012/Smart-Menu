# Telemetry Bug Report — Chaos QA Swarm

**Target**: https://smart-menu-sigma.vercel.app
**Date**: 2026-07-04

## Summary

| Metric | Value |
|--------|-------|
| Total Failures | 6 |
| Fixed | 4 |
| Unresolved | 2 |

## Failure Log

| # | Route | Selector | Error | Status |
|---|-------|----------|-------|--------|
| 1 | https://smart-menu-sigma.vercel.app | page | ## Audit Report: smart-menu-sigma.vercel.app  ### 1. Page Accessibility **Mixed — some issues found:** - Page has proper heading structure (h1-h4), landmark regions (banner, navigation, contentinfo), and meaningful alt text on images. - **WARNING**: `aria-hidden` applied to an ancestor `<div>` while its descendant retained focus. The focused `<input>` had a `border-destructive` class visible (likely validation error state), and its ancestor was aria-hidden, hiding it from screen readers. This violates ARIA spec. Use `inert` instead. - Language is Arabic (`dir="rtl"` `lang="ar"`) — appropriate.  ### 2. Console Errors - **1x 401 (POST /api/auth/login)** — Expected. Login attempt with invalid credentials. Auth endpoint correctly returns 401. - **1x 403 (POST /api/restaurants)** — **Real issue**. The subscribe form submitted empty fields `{"name":"","slug":"","planId":86,"username":"","password":""}` to `/api/restaurants` which returned 403 Forbidden. The form validation should have caught empty fields before submission — either client-side validation missed it or was bypassed. The 403 from the API itself may be intentional (CSRF/rate-limit), but the empty-field submission is a validation gap. - **No JS runtime errors** (no uncaught exceptions, undefined references, etc.).  ### 3. Failed Network Requests - **POST /api/restaurants** → **403** (empty field submission, no validation guard) - **POST /api/auth/login** → **401** (test credentials, expected behavior) - All other requests (75+ total): **200/201/206** — all static assets, RSC payloads, fonts, API calls return successfully. No broken images, missing CSS, or 404s.  ### 4. Interactive Element Test Results  \| Element \| Action \| Result \| \|---\|---\|---\| \| Dark mode toggle (الوضع الليلي/النهاري) \| Click \| Works — toggles between night/day mode \| \| Hero CTA "أنشئ قائمتك مجاناً" \| Click \| Navigates to `/subscribe` \| \| Hero CTA "تواصل عبر واتساب" \| Click \| Opens WhatsApp link \| \| Testimonial carousel buttons (السابق/التالي) \| Click \| Carousel cycles through testimonials — works \| \| Testimonial dot indicators (التقييم 1-5) \| Click \| Works — jumps to specific testimonial \| \| Client brand buttons (SOHO, Telepizza, The Cheese, Empire, Kubaba) \| Click \| Works — switches testimonial in second carousel \| \| Billing toggle (شهري/سنوي) \| Click \| Works — switches prices correctly (19/mo → 190/yr) \| \| FAQ disclosure triangles \| Click \| Expands/collapses content correctly \| \| Subscribe plan selector \| Click \| Selects plan correctly \| \| Subscribe form — fill & submit \| Fill + Click \| Opens payment dialog (correct flow) \| \| Payment dialog — fill phone & amount \| Fill \| Accepts input \| \| Payment submit \| Click \| **403 error** — empty fields submitted before validation \| \| Login form — fill & submit \| Fill + Click \| 401 (expected — wrong credentials) \| \| Password show/hide toggle \| Click \| **Not tested individually** but button exists \| \| /demo link \| Navigate \| Redirects to `/login` (expected — v0 path redirect) \|  ### 5. Summary of Findings  **Critical:** - **Subscribe form submits empty fields** → triggers 403 on `/api/restaurants`. No client-side validation prevents empty submission. Users could see a broken "Forbidden" response rather than helpful validation messages.  **High:** - None found — all core flows (navigation, pricing, subscription, dark mode, testimonials) function without JS errors or broken resources.  **Medium:** - **Accessibility violation**: `aria-hidden` on ancestor with focused child input (the validation-state input has `border-destructive` and `ring-destructive` classes, suggesting a previous validation error, but when focused it becomes invisible to AT because of the aria-hidden ancestor).  **Low/Nit:** - All font files, images, static chunks, and API endpoints load successfully (200/206). - No 404s, no broken images, no missing stylesheets. - Vercel Insights script loads correctly. - PWA manifest (`/manifest.json`) loads correctly. | ❌ |
| 2 | https://smart-menu-sigma.vercel.app/login | page | Now I have all the data needed for a complete audit. Let me summarize.  ## Page Audit Report: https://smart-menu-sigma.vercel.app/login  ### 1. Critical Bug: SPA Client-Side Redirect from `/login` In Chrome DevTools (non-isolated context), navigating to `/login` immediately redirects to `/` — the login page never renders. The homepage is displayed instead. This is a **client-side redirect** (likely middleware or Next.js auth logic reading a cookie/CSRF token that exists). Even in a fresh isolated context (no cookies), it sometimes redirects.  **Root cause**: The presence of a `csrf-token` cookie (present in the default tab) triggers a redirect to `/`. The app seems to check for any session-related cookie and redirects authenticated users away — which is expected behavior IF the user is logged in, but the `csrf-token` alone should not signal "authenticated." A CSRF token is not a session token.  **Workaround to test**: Using Playwright's new context (truly fresh) loads the login page correctly.  ### 2. Page Snapshot (login form) Present via Playwright (fresh session): - Header: Logo "الربط الذكي", subtitle "لوحة تحكم المطاعم", "العودة للرئيسية" link, dark mode toggle button - Form: "اسم المستخدم" textbox, "كلمة المرور" password textbox, "إظهار كلمة المرور" toggle button, "تسجيل الدخول" submit button - Footer: "نظام إدارة المطاعم — الربط الذكي"  ### 3. Console Errors **One error (expected)**: 401 from login API with invalid test credentials — not a bug, correct behavior. **One verbose warning**: Input elements should have autocomplete attributes (suggested: "current-password") — **minor accessibility/UX improvement needed**.  ### 4. Network Errors - `POST /api/auth/login` → **401** (expected — invalid credentials test). - All other requests: 200 OK. - No 4xx/5xx for static assets or API endpoints. - Prefetch requests (`?_rsc=...`): All 200.  ### 5. Accessibility - **Page has proper RTL structure** with Arabic labels, headings hierarchy. - **Missing autocomplete attributes** on password field (verbose warning). - Buttons have identifiable names.  ### 6. Interactive Elements Tested  \| Element \| Action \| Result \| \|---\|---\|---\| \| **اسم المستخدم** textbox \| Fill "test@test.com" \| OK \| \| **كلمة المرور** password field \| Fill "Test1234" \| OK \| \| **إظهار كلمة المرور** toggle \| Click → shows password \| OK — text visible \| \| Password field after toggle \| Click again → retype password \| OK — input remains password type but value preserved \| \| **تسجيل الدخول** button \| Click with invalid creds \| **401 returned**, error message shown: "اسم المستخدم أو كلمة المرور غير صحيحة" \| \| **العودة للرئيسية** link \| Click \| Navigates to `/` — OK \|  ### 7. Findings Summary  **Bug (medium severity)**: `/login` URL redirects to home page when `csrf-token` cookie exists. The CSRF token should not be treated as an auth session indicator. This breaks the "لوحة التحكم" nav link for anyone who has ever visited the site (since the CSRF token is set on first page load). When users click "لوحة التحكم" expecting a login form, they see the homepage instead with no indication of why.  **Minor**: Missing `autocomplete` attribute on password input (suggested: `current-password`).  **No broken behavior**: Form submits work. Password toggle works. Dark mode toggle present. All network requests 200 except the deliberately-tested 401. No JS errors. | ✅ [FIXED] |
| 3 | https://smart-menu-sigma.vercel.app/pricing | page | ## Audit Report: https://smart-menu-sigma.vercel.app/pricing  ### 1. Page Does Not Exist at /pricing **BUG**: `/pricing` immediately redirects to `/subscribe` (the registration/onboarding page). No standalone pricing display exists at this URL. The nav link "الخطط والأسعار" leads directly to the registration form, not a page that shows pricing plans for comparison.  ### 2. Console Errors **1 error** found: - `POST /api/restaurants → 422 (Unprocessable Entity)`  Request body was all empty strings: `{"name":"","slug":"","description":"","phone":"","whatsapp":"","planId":86,"username":"","password":""}`  **BUG**: The registration form renders with pre-filled text ("مقهى الواحة", "al-waha-cafe", "0910089975", "admin") in the snapshot, but the actual POST sends empty fields. This is a **React hydration mismatch** -- the SSR-rendered HTML shows placeholder/default values, but the controlled React component state never received these values, so clicking "إنشاء الحساب والبدء" submits empty data. `ponytail: root cause is missing initial state hydration for controlled inputs; fix by initializing form state from server-rendered defaultValues or using uncontrolled inputs with defaultValue for pre-filled demo fields.`  ### 3. Network Requests - API: `GET /api/plans` → 200 (loads pricing plans) - API: `GET /api/config` → 200 - API: `POST /api/restaurants` → **422** (bug #2) - API: `GET /api/public/stats` → 200 - All static assets (JS chunks, CSS, fonts, images): 200 - All `_rsc` streaming RSC fetches: 200 - Vercel Insights/SpeedInsights scripts: 200  **No 4xx/5xx** other than the 422 POST.  ### 4. Accessibility - Landmarks present: banner, navigation, contentinfo, region (notifications), alert - Heading hierarchy: h1 → h2 → h3, no skips - All interactive elements have accessible labels - Dark mode toggle present and functional - Form inputs have labels - Toast with "بيانات غير صالحة" uses `role="alert"` (good) - **Passable but not audited in depth** (no axe/lighthouse run on this page)  ### 5. Interactive Elements Tested  \| Element \| Action \| Result \| \|---\|---\|---\| \| "الوضع الليلي" (dark mode toggle) \| Click \| Toggles theme, navigated to /privacy (previous navigation artifact) \| \| "تغيير" (Change plan) \| Click \| Navigated back to plan picker, works \| \| "مجاني" plan card \| Click \| Selected free plan \| \| "أساسي" plan card \| Click \| Selected basic plan \| \| Form text inputs \| Pre-filled in DOM \| Submitted as empty (hydration bug) \| \| "إنشاء الحساب والبدء" submit \| Click \| 422 error, toast "بيانات غير صالحة" \|  ### Summary of Issues Found  1. **/pricing missing** -- navigation link points to a URL that redirects; no actual pricing page 2. **Hydration bug** -- form displays data but submits blanks, causing the 422 on `/api/restaurants`. Likely introduced when pre-fill defaults were added to the SSR template but not wired to the client component's useState initializer. 3. All JS, CSS, fonts, images, and API endpoints (except the one bug) load correctly. Vercel cache hits for most assets. | ❌ |
| 4 | https://smart-menu-sigma.vercel.app/subscribe | page | Now I have all the data I need. Let me compile the audit report.  Here's the complete audit:  **Page URL**: https://smart-menu-sigma.vercel.app/subscribe  ---  **1. Page Rendering**: The page renders successfully. It's a multi-step subscription flow: (1) select plan - Free (0 LYD) or Basic (19 LYD/month), (2) restaurant info form with pre-filled demo data.  **2. Accessibility**:  - Has proper ARIA landmarks (banner, navigation, contentinfo) - Form fields have labels - Has a `role="alert"` live region - Dialog has `role="dialog"` with aria-description - **Issue**: There are duplicate `aria-hidden` warnings in console. A `div` with `aria-hidden` was found to have a focused descendant, which blocks screen readers. This is an accessibility violation — elements with `aria-hidden` should not contain focusable content. - **Issue**: The "اختر خطة أولاً" button starts disabled with no explanation of why it's disabled (i.e., you have to select a plan first). The `disabled` state lacks an `aria-disabled` with description pattern.  **3. Console Errors:** - **None on initial page load** — zero errors. Good. - **On form submit (422)**: `Failed to load resource: the server responded with a status of 422 ()` — this is the POST to `/api/restaurants`. - **On redirect to /login (401)**: `Failed to load resource: the server responded with a status of 401 ()` — auto-login attempt after subscription flow redirects to `/login`. - **Warning**: `Blocked aria-hidden on an element because its descendant retained focus.` — accessibility bug.  **4. Failed Network Requests:** - **POST /api/restaurants → 422** when submitting with pre-filled defaults (empty field values sent) - **POST /api/auth/login → 401** when redirected to login after subscription flow  **5. Routing Issues:** - `/subscribe` **without query params** inconsistently redirects to `/`, `/pricing`, or `/terms` (client-side redirect).  - `/subscribe?plan=86` (with plan query param) renders correctly. - This means the "ابدأ الآن" / "اشترك الآن" CTAs that link to bare `/subscribe` may fail for some users.  **6. Form Submission Bug (Critical):** - The POST to `/api/restaurants` sent `{"name":"","slug":"","description":"","phone":"","whatsapp":"","planId":86,"username":"","password":""}` — **all empty strings** — even though the DOM inputs showed pre-filled values ("مقهى الواحة", "al-waha-cafe", etc.). - Root cause: React form state is initialized with default values but the default values are not synced to controlled form state until the user interacts with the fields. - When fields are **manually filled** by the user, the submission succeeds (opens payment dialog).  - Workaround: user must type in at least one character in each field.  **7. Interactive Element Tests:** - ✅ Plan selection (مجاني / أساسي) — works, label changes to "اخترت ..." - ✅ "تغيير" (change) button — returns to plan selection - ✅ Form field filling — all inputs accept text - ✅ Form submission with manual fill — opens payment dialog (Libyana/Madar) - ✅ Payment method toggle (ليبيانا / مدار) — switches provider info - ✅ Dark mode toggle — works (html class `dark` toggles) - ✅ Nav links — all navigate correctly - ❌ **Form submit with pre-filled defaults fails (422)** — as documented above - ❌ **Bare `/subscribe` route inconsistent** — redirects to wrong pages  **8. Summary of Findings:**  \| Severity \| Finding \| \|----------\|---------\| \| High \| Form submission with pre-filled default values sends empty fields → 422 error \| \| Medium \| `/subscribe` route without `?plan=X` inconsistently redirects to wrong pages \| \| Low \| Aria-hidden on ancestor of focused element blocks screen reader access \| \| Info \| 401 on `/api/auth/login` is expected side-effect of redirect to `/login` after subscription \|## Audit Report: https://smart-menu-sigma.vercel.app/subscribe  ### Findings  \| Severity \| Category \| Finding \| \|----------\|----------\|---------\| \| **HIGH** \| **Bug** \| **Form submission sends empty fields when using pre-filled defaults.** POST 422 on `/api/restaurants` — request body was `{"name":"","slug":"","description":"","phone":"","whatsapp":"","planId":86,"username":"","password":""}` even though DOM inputs displayed "مقهى الواحة", "al-waha-cafe", etc. React state doesn't sync default values until user interacts. User must type in every field manually. \| \| **HIGH** \| **Route** \| **Bare `/subscribe` inconsistently redirects** to `/`, `/pricing`, `/terms`, or `/login`. Only `?plan=X` query-param variant (from `/pricing` CTAs) renders correctly. Breaches "ابدأ الآن" nav link which points to bare `/subscribe`. \| \| **MEDIUM** \| **Bug** \| **Form error UX is silent.** After 422, page silently reverts to step 1 (plan selection). No toast, no error message — user sees no indication of failure. \| \| **LOW** \| **A11Y** \| `aria-hidden` on ancestor of focused input — blocks screen reader. `aria-hidden` element must not contain focusable descendants. \| \| **INFO** \| **Network** \| POST `/api/auth/login` → 401 — expected; auto-login attempt after subscription redirects to `/login`. \| \| **OK** \| **Network** \| All 65 page-load requests returned 200. Zero console errors on clean page load. \| \| **OK** \| **Dark mode** \| Toggle works. \| \| **OK** \| **Plan selection** \| Works. Button labels update correctly. \| \| **OK** \| **Payment dialog** \| Works. Libyana/Madar switch, copy buttons, amount field, tel: links all functional. \|  ### Screenshots  - Subscribe page (clean state): attached above - Payment dialog (after submit): attached above - Dark mode: verified functional via JS (`html.classList.contains('dark')` → `true`) | ✅ [FIXED] |
| 5 | https://smart-menu-sigma.vercel.app/terms | page | ## Audit Report: `https://smart-menu-sigma.vercel.app/terms`  ### 1. Page Redirect Behavior  **CRITICAL**: `/terms` **does not exist** as a page. Navigating to `https://smart-menu-sigma.vercel.app/terms` silently redirects to `/` (the homepage). The page title reads "الربط الذكي \| Smart Menu — منيو رقمي للمطاعم والمقاهي", not a terms page.  Two consecutive navigations to `/terms` both landed on the homepage with no terms content rendered. A third navigation (via the footer link) resolved to `/privacy`.  ### 2. Console Messages  **No console errors or warnings.** Clean.  ### 3. Failed Network Requests  **None.** All 67 requests returned 200 (or 206 for the MP4 partial content). No 4xx/5xx.  ### 4. Page Accessibility  **Mixed.** - Positive: proper heading hierarchy (h1-h4), banner role, contentinfo role, navigation role, ARIA regions, alt text on images - Negative: "الوضع الليلي" toggle has no accessible label (just a generic image), social media links marked "قريباً" (coming soon) have descriptive text, no ARIA live region for the testimonial carousel  ### 5. Interactive Elements Tested  \| Element \| Action \| Result \| \|---\|---\|---\| \| Night mode toggle \| Click \| Toggled successfully (label changed from "الوضع الليلي" to "الوضع النهاري") \| \| Testimonial carousel "التالي" \| Click \| Moved to next testimonial \| \| Testimonial carousel "السابق" \| Click \| Moved to previous testimonial \| \| Testimonial dot buttons "التقييم 1-5" \| Click \| Navigated to specific testimonials \| \| Brand testimonial buttons (SOHO, Telepizza, etc.) \| Click \| Switched brand testimonials correctly \| \| Subscribe plan "أساسي" (19 د.ل/month) \| Click \| Opened payment dialog correctly \| \| Payment dialog - amount field \| Fill \| Accepts numeric input \| \| Payment dialog - phone field \| Fill \| Accepts phone number \| \| Payment toggle "ليبيانا" / "مدار" \| Click \| Switches payment methods \| \| Footer "شروط الاستخدام" link \| Click \| **Navigated to `/privacy` (privacy page), NOT a terms page** \|  ### 6. Key Findings  1. **MISSING ROUTE - `/terms` redirects to `/`**. The Terms of Service page is never rendered. The route either doesn't exist or redirects to the homepage. 2. **WRONG LINK - Footer "شروط الاستخدام" (Terms of Use) points to `/privacy`**, the Privacy Policy page. Both footer links ("شروط الاستخدام" and "سياسة الخصوصية") link to the same `/privacy` URL. 3. No JavaScript console errors. 4. No failed network requests (all 200s/206s). 5. Payment dialog and carousel interactive elements function correctly with no errors. 6. Night mode toggle works bidirectionally. | ✅ [FIXED] |
| 6 | https://smart-menu-sigma.vercel.app/privacy | page | ## Audit Report: https://smart-menu-sigma.vercel.app/privacy  ### Page loaded successfully - Status: 200 OK - Title: "سياسة الخصوصية \| المنيو الذكي" - Content: Full privacy policy with 5 sections (جمع المعلومات, استخدام المعلومات, حماية البيانات, جهات خارجية, اتصل بنا), last updated June 2026  ### Console errors - **0 errors** - 1 minor warning: a `<link rel=preload>` for `1iz8kmo2kjqks.css` was loaded speculatively but not used within a few seconds. Harmless — unused preload, no functional impact.  ### Network requests (page-specific) - All 66 requests returned **200** — no 4xx, no 5xx - Zero images, fonts, scripts, or API calls failed  ### Accessibility - **Good**: Proper heading hierarchy (h1 `سياسة الخصوصية` → h2 for each section), `banner` and `contentinfo` landmarks, nav labeled `الرئيسية`, all images have alt text, ARIA live regions for notifications - **Minor**: Two duplicate dark-mode toggle buttons (one in nav bar `49_13`, one floating `49_18`) — the duplicate is a redundant control, not a WCAG failure - No keyboard-trap or focus issues detected  ### Interactive elements test  \| Element \| Action \| Result \| \|---\|---\|---\| \| Dark mode toggle (`الوضع الليلي`) \| Click \| **Label changed to `الوضع النهاري`** — toggle works. However, click triggers SPA route change to `/subscribe` as a side effect. **Potential bug**: Next.js theme-switcher may be triggering a full re-render/redirect instead of a client-side CSS toggle. \| \| Back-to-top button \| Present \| Present in DOM but not triggered (already at top) \| \| Nav links (login, pricing, subscribe) \| Visible \| All point to valid routes \| \| Footer links (terms, privacy) \| Visible \| Both present \|  ### Verdict **CLEAN** — no console errors, no failed network requests, accessible structure.   **One finding**: The dark mode toggle button (`الوضع الليلي`) on the `/privacy` page triggers an unwanted SPA navigation to `/subscribe` or `/` after click. The toggle functionally works (label changes to `الوضع النهاري`), but the page redirect is not expected behavior. This may be a Next.js App Router state-commit issue where clicking the theme toggle triggers a `router.push()` or a full route remount somewhere in the layout provider. Should be investigated in the theme provider component — the toggle should only update CSS var/class, not push a route. | ✅ [FIXED] |

---
_Generated by Chaos QA Swarm_
