# تقرير الفحص الشامل — محاكاة مستخدم كاملة

**التاريخ:** 2026-08-01
**النطاق:** 54 وكيل محاكاة (17 اكتمل بنجاح، 85 فشل بـ 429 rate limit — حصة API)
**الفحوصات:** 565 فحص ناجح + تحقق يدوي curl للنتائج الحرجة

## الملخص

| الخطورة | العدد |
|---------|-------|
| CRITICAL | 8 |
| HIGH | 35 |
| MEDIUM | 82 |
| LOW | 78 |
| **المجموع** | **203** |

| الفئة | العدد |
|-------|-------|
| bug | 45 |
| content | 39 |
| security | 33 |
| missing | 26 |
| a11y | 19 |
| improvement | 16 |
| perf | 9 |
| duplicate | 7 |
| ui-deformity | 5 |
| rtl | 4 |

---

## 🔴 حرجة (8) — مؤكدة يدوياً بالـ curl

### 1. `/api/restaurants` يسرّب بيانات المالكين بلا مصادقة
- **الدليل:** `curl https://menu.smart-link.ly/api/restaurants` → 200 بدون أي كوكي، يعيد كل المطاعم (163) مع plan/planEnd/maxItems/maxOrders/phones
- **الخطورة:** أي زائر يرى خطط واشتراكات وأرقام جميع المطاعم
- **الإصلاح:** requireAuth على المسار، أو مسار عام sanitized عبر `/api/public/*`

### 2. إرسال تقييمات بلا مصادقة + CSRF ضعيف (Origin فقط)
- **الدليل:** `POST /api/items/742/reviews` بدون Origin → 403، مع Origin مزيف → **201** — أي موقع خارجي يرسل تقييمات 5 نجوم عشوائية
- **الإصلاح:** جلسة أو nonce + rate limit لكل عنصر/IP + توحيد المخطط

### 3. `/demo` يمنح جلسة owner مدفوعة حقيقية!
- **الدليل:** `GET /demo` → 307 + Set-Cookie `smart-menu-session` حقيقي + `Smart-menu-restaurant=102`، ثم POST /api/subscriptions بها → إنشاء اشتراك معلق حقيقي (id 172) يدخل طابور موافقة Telegram في الإنتاج
- **الخطورة:** أي زائر يطلب اشتراكاً مدفوعاً مجاناً — ثغرة مالية حقيقية
- **الإصلاح:** حذف /demo من الإنتاج أو جعله read-only على مستأجر sandbox

### 4. سر Telegram webhook الحقيقي مكتوب في ريبو عام
- **الدليل:** `tests/security/webhook-security.test.ts` يحتوي السر الحقيقي، الريبو PUBLIC، والـ webhook الحي يقبل طلبات به (200)
- **الخطورة:** أي شخص يستطيع اعتماد مدفوعات Telegram أو إرسال سبام عبر البوت
- **الإصلاح:** **تدوير فوري** لـ TELEGRAM_WEBHOOK_SECRET في Vercel + حذف السر من الاختبارات (اقرأ من env)

### 5. sitemap.xml ملوّث بـ 150+ slug اختبار وقبيح
- **الدليل:** `/menu/sex`, `/menu/nigga-com`, `/menu/----`, `/menu/qqqq` — كلها 200
- **الخطورة:** حرق crawl budget، فهرسة بيانات اختبار، ضرر سمعة
- **الإصلاح:** فلترة sitemap للمطاعم النشطة فقط + رفض slugs بذيئة عند الإنشاء

### 6. صفحة /menu (تصفح المطاعم) قشرة تحميل فارغة
- **الدليل:** HTML يحتوي "جاري التحميل..." فقط، صفر h1/h2/img/روابط، مع meta robots index,follow
- **الخطورة:** محركات البحث ترى صفحة فارغة
- **الإصلاح:** SSR لقائمة المطاعم + h1 دلالي

### 7. `/api/restaurants` يعيد 163 مطعماً منها 76 اختبار/قمامة
- **الدليل:** slugs مثل `cafe-t1782687922`, `test-full-ts`, `admin-test-1784225591`, `restaurant-170`
- **الإصلاح:** فلترة isActive + استبعاد أنماط الاختبار

### 8. `/api/config` يكشف إعدادات المنصة الداخلية بلا مصادقة
- **الدليل:** `GET /api/config` → 200: platform_fee_pct, min_withdrawal, wallet_provider
- **الإصلاح:** requireAuth + دور admin

---

## 🟠 عالية (35) — أبرزها

### أمان
- webhook /start أداة سبام مجانية عبر البوت (بعد تسريب السر)
- لا ترويسات rate-limit في أي API (16 من 61 مساراً فقط عليه حماية)
- تناقض نموذج المصادقة: config 200 بلا جلسة بينما restaurants/1 401

### محتوى / تسويق
- ادعاء "أكثر من 500 مطعم" بينما الإحصاء الفعلي 163
- FAQ يعد بـ"أصناف غير محدودة" لا توجد في أي خطة (الأقصى 50)
- أرقام واتساب placeholder (`218911111111`) في الهيدر والفوتر بينما الزر العائم يستخدم الرقم الصحيح
- أيقونات سوشيال ميتة "قريباً" غير قابلة للنقر
- نفس صورة main21.png مستخدمة مرتين في الـ hero

### أسعار
- **Premium (29 د.ل) متفوقة تماماً على Pro (129 د.ل)** — سلم أسعار مقلوب! sortOrder متصادم
- شبكة الخطط client-rendered بالكامل (SSR يعرض 4 هياكل عظمية فقط)
- صفحة /subscribe تنزل إلى spinner (BAILOUT_TO_CLIENT_SIDE_RENDERING)

### API
- `/api/items?restaurantId=N` يرفض 400 رغم أنه المستخدم الفعلي بالموقع (خلل توثيقي)
- `/api/restaurants?slug=` يتجاهل الفلتر
- تقديم categoryId وrestaurantId معاً يفشل 400

### SEO
- لا canonical على أي صفحة
- og:url مثبت على الصفحة الرئيسية في 7+ صفحات
- twitter card يتعارض مع og
- apple-touch-icon 404 على كل صفحة
- صفحات cart/login/order-confirmed قشور فارغة في الـ sitemap
- صفحة الطباعة: عنوانان متعارضان + ليست noindex

### إدارة
- صفحة Admin Menu فارغة دائماً (fetch ينقص restaurantId)
- AdminEventNotifier لا يعمل (شكل الـ envelope مختلف)
- حفظ إعدادات Telegram يكتب حروف الـ mask "•••" كقيمة حقيقية للتوكن

---

## 🟡 متوسطة (82) — خلاصة

- واتساب بقائمة المطاعم يستخدم رقماً محلياً بلا رمز دولة → wa.me معطل
- `mr-1` فيزيائي يكسر RTL في مبدل الفوترة بالأسعار
- تقييمات مزيفة "SOHO" في الشهادات (أسماء مختلقة كعملاء حقيقيين)
- لا ربط aria بين الحقول والأخطاء في نماذج الاشتراك
- 508KB PNG رئيسي دون WebP/AVIF
- dots "التقييم 1..7" لشريط شهادات واحد
- عناوين خاطئة في صفحات متعددة

---

## ✅ ما يعمل (284 تمرير)

- صفحة المنيو الحية `/menu/al-waha-cafeewewe`: عناصر + فئات + بحث + فرز + تقييم + ولاء + مشاركة + طباعة — كلها سليمة RTL
- خطط API: 5 خطط موجودة
- تسجيل الدخول: مصادقة صحيحة، كلمات مرور PBKDF2
- نقاط الولاء + الإحالات
- تدفق Telegram approve/reject يعمل (مع السر المسرَّب)
- تدفق الطلب → واتساب → تأكيد
- الحماية من CSRF في معظم مسارات التغيير (عبر Origin)

---

## الأولوية المقترحة

1. **فوري (أمن/مالي):** تدوير TELEGRAM_WEBHOOK_SECRET + حذف /demo من الإنتاج + requireAuth على config/restaurants + تقييمات بنونس
2. **قبل الإطلاق:** sitemap نظيف + SSR لـ /menu + إصلاح سلم الأسعار المقلوب + توثيق /api/items?restaurantId
3. **تحسينات:** canonical + og:url ديناميكي + apple-touch-icon + توحيد رقم واتساب + صورة hero أخف
