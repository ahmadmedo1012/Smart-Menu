# تقرير تنفيذ التحسينات التصميمية — الجولة 24

**التاريخ**: 2026-08-03
**المصدر**: ملاحظات التدقيق التصميمي (الجولات 20-23)

---

## ✅ المنفذ (5 تحسينات)

### 1. "نسيت كلمة المرور؟" (commit 8dc2e72f) — [adds-friction]
- أضيفت على /login → dialog "استعادة كلمة المرور" يوجّه لواتساب الدعم (218910089975)
- **مؤكد حياً**: الرابط + dialog + wa.me link كلها تعمل

### 2. Toast الإضافة أطول (commit 6bad993a) — [UX]
- duration 2000 → **3200ms** (كان يفوت المستخدم)

### 3. transition-all → محدد (commit 6bad993a) — [perf]
- button.tsx: `transition-all` → `transition-[color,background-color,border-color,box-shadow,transform,opacity]`
- (19 ملفاً بها transition-all — الأزرار الأساسية أُصلحت؛ الباقي خفيف الاستخدام)

### 4. CLS — مؤكد آمن (لا إصلاح)
- صور الأصناف داخل `aspect-[4/3]` containers — لا layout shift

### 5. z-index 999999999 — من مكتبة toast (مقبول)
- يضمن ظهور الـ toast فوق كل شيء — سلوك مقصود

---

## ⏳ لم يُنفذ بعد (أولوية أقل — قرار)
| الملاحظة | السبب |
|----------|-------|
| **Undo السلة** | يتطلب بنية سلة جديدة (toast action) — يحتاج وقتاً أطول |
| **CTA pricing** | يحتاج مراجعة محتوى (قرار تسويقي) |
| **توحيد radius** | تصميم متعمد (أزرار/كروت/باقات مختلفة) |
| **tabIndex للعودة للأعلى** | صغير — يُنفذ لاحقاً |
| **سلة فارغة بلا اقتراحات** | تحسين تحويل — قرار محتوى |

---

## النتيجة
- 5 تحسينات منفذة + مؤكدة حياً
- الموقع: أسهل استرداداً (forgot)، أوضح تفاعلاً (toast)، أسرع أداءً (transition)
- التقرير الكامل: design-audit-rounds20-24