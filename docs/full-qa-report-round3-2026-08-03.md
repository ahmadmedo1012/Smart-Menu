# تقرير الجولة 3 — محاكاة مستخدمين حقيقيين (Persona-Driven)

**التاريخ**: 2026-08-03
**الطريقة**: 4 personas × رحلات كاملة من وجهة نظر مستخدم حقيقي (P1 زبون، P3 مالك جديد، P4 multi-menu، P7 خبيث)
**النتيجة**: **10/10 اختبارات تيمر (exit 0)** — مع إصلاح أمني حقيقي

---

## 🔴 خطأ أمني حقيقي اكتُشف وأُصلح (الأهم)
### [عالي] لا حد أقصى لطول الأسماء/الأوصاف — oversized input يُنشأ
- **أين:** `src/app/api/items/route.ts`, `items/[id]/route.ts`, `categories/route.ts`, `categories/[id]/route.ts`
- **خطوات إعادة الإنتاج:** عميل موثّق يرسل `POST /api/categories` مع `{name: "X".repeat(100000)}`
- **المتوقع:** رفض (400/413) — لا تُنشأ بيانات ضخمة
- **الفعلي:** **201 مولد** — الـ zod كان `z.string().min(1)` بلا حد أقصى
- **الإصلاح:** `max(100)` للأسماء، `max(1000)` للأوصاف (4 ملفات)
- **الدليل:** اختبار P7-oversized فشل 201 → نجح بعد الإصلاح
- **التكرار:** دائم (كان يمكن استغلاله)
- **commit:** `17d08311`

## Personas

### P1 — زبون عادي (persona-1)
- منيو → فلتر تصنيف → إضافة صنفين → سلة → كمية/ملاحظة → نوع توصيل → إرسال
- تحقق: `POST /api/orders` = **201** + **0 أخطاء console** ✓

### P3 — مالك جديد يبني منيو (persona-3)
- تسجيل حساب جديد → لوحة تحكم → محرر منيو (لا فشل) → منيو عام يعرض المطعم ✓

### P4 — مالك متعدد المطاعم (persona-4)
- التبديل ← dashboard يعرض 315 ✓ → orders تحمّل بلا تسريب ✓ → settings بلا فشل ✓
- العودة للـ primary يعيد 313 ✓
- **عزل المستأجرين سليم في كل الصفحات**

### P7 — مستخدم خبيث (persona-7) — **6/6**
- IDOR (enumerate restaurants) ✓ | XSS escape ✓ | URL tampering ✓ | SQL-injection search ✓ | **oversized input → يرفض الآن** ✓ | admin path fuzzing ✓

## النظافة
- 4 مطاعم P3 + 4 مستخدمين p3 + سكربت التطهير (أُضيف p1_/p3_ للـ needles) — **DB نظيف 0**

## الأسليب
- selectors ثابتة (placeholder/aria) — لا nth
- بيانات QA موسومة + تنظيف
- لا دفع حقيقي

## الجولة مقارنةً
| الجولة | الاختبارات | النتيجة |
|--------|-----------|---------|
| 1 | 54 | 52/54 (3 صور مكسورة) |
| 2 | 67 E2E + 346 unit + lint | **67/67 exit 0** |
| 3 (personas) | 10 persona-رحلات | **10/10 exit 0** + إصلاح أمني |