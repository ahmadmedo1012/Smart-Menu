# تقرير الجولة 11-12 — إدارة المنيوهات + مسار الوهمي

**التاريخ**: 2026-08-03

---

## الجولة 11 — `/owner/menus` redirect (commit 507fa3bf) ✅
- **اكتشاف**: `/owner/menus` (غير موجود) كان يعرض fallback "منيو تجريبي" بدل 404
- **إصلاح**: redirect → `/owner/restaurants` (المدير الحقيقي)
- **مؤكد حياً**: يوجّه الآن ✓

## الجولة 12 — إدارة المنيوهات العميقة (5/6 + تأكيد)
- الـ manager يعرض المنيوهات الثلاثة ✓ + "إضافة منيو جديد" ✓
- **زر الإضافة يفتح form** ("منيو جديد" + حقول) — السلوك صحيح (الحد يُفرض عند الحفظ API 403، مؤكد سابقاً)
- **تعيين أساسي** يعمل ✓
- أزرار حذف موجودة (16) ✓
- (الفشل الوحيد: اختبار توقع رسالة حد عند الزر — التصميم يفتح form ثم يرفض API. لا خطأ.)

## الخلاصة
لا أخطاء في إدارة المنيوهات. كل الوظائف تعمل: عرض/إضافة (form)/تعيين أساسي/حذف. حد الخطة مفروض في الـ API. المسار الوهمي أُصلح.