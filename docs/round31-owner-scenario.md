# تقرير الجولة 31 — سيناريو المالك الكامل + اكتشافان حرجان

**التاريخ**: 2026-08-03

---

## 🔴 اكتشافان حقيقيان أُصلحا

### 1. المنيو التجريبي (312) بلا مالك — الطلبات تضيع (commit 5e1ee316 + ربط DB)
- **الاكتشاف**: `al-waha-cafe-demo` (312) كان نشطاً يستقبل طلبات (33) لكن **لا مالك مرتبط** — الطلبات تذهب للفراغ
- **الإصلاح**: ربط 312 بحساب waha (مالك الـ demo) — يرى طلباته الآن

### 2. الـ switcher لا يزامن الصفحات (commit 5e1ee316)
- **الاكتشاف**: تبديل المطعم في dropdown كان يغيّر الاسم لكن الطلبات تبقى القديمة (useActiveRestaurant يقرأ مرة واحدة)
- **الإصلاح**: custom event `smartmenu:restaurant-change` + storage listener → إعادة resolve + refetch
- **مؤكد حياً**: بعد التبديل لـ 312 → طلب R31 يظهر ✓

### 3. 445 طلباً اختبارياً تلوّث إحصائيات 102
- **الاكتشاف**: ORD-RATE/E2E (من اختبارات rate-limit) = 445 من 500 (89%)
- **الإصلاح**: حُذفت → 55 طلباً حقيقياً

## ✅ الدورة الكاملة مؤكدة (سيناريو حقيقي)
زبون يطلب (ORD-MSDTLTXZ-BB5FA110, 15 د.ل, "طلب اختبار R31") → DB (restaurantId 312, status new) → مالك waha يبدّل مطعمه → **يرى الطلب** (1 صنف • داخل المكان • 15.0) → **يغيّر الحالة** ("قيد التحضير" ✓ + "جاهز" متاح) ✓

## ملاحظة
- المطعم 312: 33 طلباً (كلها حقيقية/اختبارات حديثة) — نظيف
- المطعم 102: 55 طلباً حقيقياً بعد التنظيف

## الخلاصة
المنهجية (محاكاة سيناريو كامل) كشفت: طلبات ضائعة بلا مالك + مزامنة مكسورة + بيانات ملوثة — كلها أُصلحت. دورة الطلب الكاملة تعمل من الزبون حتى تغيير الحالة.