# تقرير الجولة 34 — الولاء: اكتشاف حرج + إصلاح + تأكيد كامل

**التاريخ**: 2026-08-03

---

## 🔴 خطأ حرج اكتُشف وأُصلح (commit 4845e56d)

### الزبون الجديد لا يكسب نقاطاً أبداً
- **السبب الجذري**: كود استحقاق النقاط في `orders/[id]/route.ts` كان `findUnique` فقط للبطاقة — **بلا create/upsert**
- **النتيجة**: أول طلب مكتمل بهاتف جديد (بلا بطاقة) = 0 نقاط بصمت — البطاقة لا تُنشأ أبداً
- **الإصلاح**: `upsert` — ينشئ البطاقة (مع referralCode مولّد `RF<restId><rand>`) عند أول طلب مكتمل، ويزيد النقاط للموجودة
- **مؤكد حياً**: بطاقة 126 (0928756821, 1 نقطة, bronze, RF312DX9MX6) + معاملة id 55 ✓

## 🔴 تعارض ميزات اكتُشف وأُصلح (commit f9649b05)

### إزالة حقل الهاتف قتلت الولاء
- **السبب**: النقاط تتطلب `customerPhone` (البطاقة مرتبطة بهاتف+مطعم) — إزالة الحقل (جولة 28 بطلب المستخدم) = صفر نقاط
- **الإصلاح**: حقل هاتف **اختياري واضح** "لكسب نقاط الولاء" — من يريد يملؤه، والباقي يتخطى

## ✅ الدورة الكاملة مؤكدة (محاكاة حية)
زبون يطلب بهاتف (0928756821) → طلب 651 → owner يكمل (قيد التحضير ← جاهز ← مكتمل) → **بطاقة أُنشئت (126) + نقطة 1 + معاملة (55)** ✓

## ملاحظات
- ptsEarned = floor(total/10) — طلب 15 د.ل → 1 نقطة (صحيح)
- الواجهة: حقل الهاتف بعلامة ولاء واضحة + dir=ltr للأرقام

## الخلاصة
الولاء يعمل الآن من الألف للياء: طلب بهاتف → اكتمال → بطاقة تلقائية → نقاط → معاملة مسجلة.