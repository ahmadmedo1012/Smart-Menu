# الجولة 69 — تدفق التسجيل (onboarding) كامل من الصفر

**التاريخ**: 2026-08-06

---

## ✅ مؤكد (فحص حي + DB)
### Onboarding الكامل
1. subscribe → اختيار "مجاني" → "اخترت مجاني"
2. خطوة 2: **7 حقول** واضحة (اسم/رابط/وصف/هاتف/واتساب/مستخدم/كلمة)
3. submit "إنشاء الحساب والبدء" → **انتقال /owner dashboard** ✓
4. **DB مؤكد**: user id 616 + مطعم `test-onb-294513` مرتبط

## الخلاصة
التسجيل من الصفر يعمل سل: free plan → form → account → restaurant → dashboard. تدفق owner جديد كاملٌ بلا عيوب — خطوة الـ 7 حقول كلها بمساعدة place-of عربي واضح.

(بيانات الاختبار المؤقتة test-onb-* ستُحذف لاحقاً تنظيفاً)