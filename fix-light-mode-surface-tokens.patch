diff --git a/src/app/globals.css b/src/app/globals.css
index a8752691..299b8ad8 100644
--- a/src/app/globals.css
+++ b/src/app/globals.css
@@ -224,6 +224,16 @@
 
 	--gradient-orange: linear-gradient(135deg, oklch(0.55 0.19 45 / 0.08), oklch(0.52 0.19 45 / 0.04));
 
+	/* Surface variants — light
+	   ponytail: these three were missing from .light entirely, so tone="alt"
+	   SectionContainers (Features/Stats/HowItWorks) inherited the dark-mode
+	   near-black --surface-sunken (0.055) at /60 opacity — a faded dark smudge
+	   over a white page. Light equivalents follow the same raised>overlay>sunken
+	   ordering as dark, mapped onto the light neutral scale instead. */
+	--surface-raised: oklch(1 0 0);
+	--surface-overlay: oklch(1 0 0);
+	--surface-sunken: oklch(0.94 0.003 0);
+
 	/* Glass — light */
 	--glass-bg: oklch(1 0 0 / 0.6);
 	--glass-bg-strong: oklch(1 0 0 / 0.8);
