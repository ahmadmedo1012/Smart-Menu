# Smart Menu — تقرير التدقيق الشامل

## ملخص
**إجمالي المكتشفات: 16** | حرج: 5 | رئيسي: 7 | ثانوي: 4

---

## 🔴 حرجة (5)

### 1. تسجيل الخلل — تسجيل الدخول التلقائي بعد إنشاء الحساب
**الملف**: `src/app/api/auth/register/route.ts:60`
**السبب**: `createSession(user.id)` يُستدعى فوراً بعد إنشاء المستخدم، فيدخل المستخدم تلقائياً بدور `USER` واشتراك `UNPAID` بدون أي تحقق.
**التأثير**: أي شخص يسجل يلقى نفسه داخل النظام.

### 2. لوحة المالك — لا تتحقق من الدور
**الملف**: `src/app/owner/layout.tsx:87`
**السبب**: الـ useEffect يتحقق فقط من `d.success` وليس `d.data.role`. أي مستخدم موثّق يشاهد واجهة المالك.
**التأثير**: مستخدم دور USER يشاهد لوحة المالك كاملة.

### 3. لوحة الأدمن — لا تتحقق من دور الأدمن
**الملف**: `src/app/admin/layout.tsx:69`
**السبب**: يقرأ `role` و `permissions` لكن لا يعيد التوجيه لو الدور ليس أدمن.
**التأثير**: أي owner أو USER يشاهد الواجهة.

### 4. الـ SSE لا يُرسل أحداث الموافقة/الرفض أبداً
**الملف**: `src/lib/subscription-decisions.ts` (عدة أسطر)
**السبب**: كل استدعاءات `prisma.systemEvent.create()` و `sendTelegramNotification()` بدون `await`. الأحداث تُسجَّل في الخلفية وقد لا تُكتب أبداً قبل قراءة الـ poll.
**التأثير**: صفحة العميل لا ترى أي تحديث SSE — لا إعادة توجيه، لا إشعار.

### 5. ترقية الاشتراك — لا إعادة توجيه بعد القبول
**الملف**: `src/app/subscribe/SubscribeForm.tsx` و `PaymentDialog.tsx`
**السبب**: بعد الموافقة، dialog ينتقل إلى شاشة "approved" لكن `onSuccess()` (الذي يعيد التوجيه) لا يُستدعى دائماً. في وضع الترقية يوجه المستخدم للوحة التحكم قبل تفعيل الخطة.

---

## 🟡 رئيسية (7)

### 6. إعادة التوجيه الافتراضية ترسل غير الأدمن إلى /admin
**الملف**: `src/app/login/page.tsx:72`
**السبب**: `redirect || "/admin"` — مستخدم USER يرسل إلى لوحة الأدمن بعد الدخول.

### 7. createSession تحذف كل الجلسات القديمة
**الملف**: `src/lib/session.ts:7`
**السبب**: `deleteExistingSessions(userId)` تجبر مستخدم واحد لكل جهاز.
**عند الإصرار على البقاء**: يعتبر سلوك مقصود — يُترك.

### 8. الـ Telegram إشعارات الموافقة/الرفض غير مضمونة
**الملف**: `src/lib/subscription-decisions.ts`
**السبب**: `sendTelegramNotification()` بدون `await` — نفس مشكلة الـ SSE.

### 9. الـ SSE في صفحة الاشتراك يستمع فقط subscription_rejected
**الملف**: `src/app/subscribe/page.tsx`
**السبب**: لا يوجد مستمع لـ `subscription_approved` — فقط الرفض عبر SSE فوري.

### 10. الـ SSE يُعيد كل الأحداث القديمة عند أول اتصال
**الملف**: `src/app/api/user/events/stream/route.ts`
**السبب**: `lastId = 0` يُرسل كل الأحداث المنطابقة لـ userId. قد يُظهر إشعارات من جلسات سابقة.

### 11. PaymentDialog — حقل المبلغ لا يتحقق من الحد الأدنى
**الملف**: `src/components/shared/PaymentDialog.tsx:193`
**السبب`: 0 أو قيمة سالبة تمر عالـ API ويرفض بخطأ غير واضح.

### 12. رسالة "success" قبل التفعيل
**الملف**: `src/components/shared/PaymentDialog.tsx`
**السبب**: عند انتهاء الـ 30 ثانية، يظهر "تم الدفع بنجاح" ويعيد التوجيه، رغم أن الدفع لم يؤكد بعد.

---

## 🟢 ثانوية (4)

### 13. لا تجديد لجلسة العمل — تنتهي بعد 24 ساعة بالضبط
**الملف**: `src/lib/session.ts:5`

### 14. استيراد verifyHash ديناميكي في كل طلب
**الملف**: `src/app/api/auth/login/route.ts:38`

### 15. logout يمسح cookie غير موجود
**الملف**: `src/app/api/auth/logout/route.ts:22`

### 16. Telegram تنظيف لوحة المفاتيح — كود ميت
**الملف**: `src/app/api/telegram/webhook/route.ts` — `metadata.telegramMessages` لم يُملأ أبداً.
